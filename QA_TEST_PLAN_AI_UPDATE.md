# CoachOS V0.2 QA 测试计划与后续工作 (AI Update)

**文档版本：** v1.1  
**最后更新：** 2026-04-03  
**QA 负责人：** Manus AI 工程师  
**状态：** 活跃

---

## 📋 已完成的 QA 工作 (V0.2 全栈化集成测试)

在最新的开发冲刺中，项目从纯前端演示升级为包含后端 API 的全栈系统。以下是新增并已通过的集成测试项：

### Phase 1: 后端 API 端点测试 ✅
- [x] `GET /api/health` - 服务健康检查返回状态、版本号及时间戳。
- [x] `POST /api/chat` - OpenAI 接口连通性，非流式响应转 Chunked SSE 输出正常。
- [x] `POST /api/chat/analyze-topic` - 话题分析逻辑正确，提取情绪与关键词无误。
- [x] `POST /api/recommend` - 咨询师匹配算法执行正常，返回加权排序列表。
- [x] `POST /api/style-analyze` - 风格分析多维度评估正常，异常降级 Fallback 逻辑生效。

### Phase 2: 前端集成测试 ✅
- [x] **Chat 页面真实对话**：发送消息后，AI Coach 能够根据 System Prompt 进行符合人设的回复，且流式打字机效果流畅。
- [x] **上下文记忆**：多轮对话中，AI Coach 能够正确引用历史聊天内容（如工作压力、身体紧张）。
- [x] **智能推荐触发**：对话超过 3 轮后，前端成功调用分析接口，并渲染推荐咨询师卡片（如李心怡 86% 匹配）。
- [x] **CounselorOnboard 流程修正**：页面文案及计时逻辑成功从 15 分钟修正为 30 分钟。
- [x] **真实风格分析报告**：30 分钟对话结束后，成功请求大模型生成多维度雷达图及分析建议，UI 渲染无错位。

---

## 🚀 接下来想要做的测试工作 (Phase 3 & Phase 4)

为了确保 CoachOS 在 V1.0 发布前达到生产级标准，我（Manus AI）为您和下一位接手的 QA 工程师整理了以下深度测试计划：

### Phase 3: 深度功能与异常测试 (待执行)

#### 1. 极端边界条件测试 (Edge Cases)
- [ ] **超长文本输入**：在 Chat 页面输入超过 2000 字符的超长文本，验证前端截断逻辑及后端 OpenAI API Token 限制处理。
- [ ] **快速连续发送**：在 AI 回复过程中连续快速点击发送按钮，验证防抖机制和请求竞态处理（是否会出现回复错乱或重复请求）。
- [ ] **特殊字符与代码注入**：输入包含 HTML/JS 代码或 Markdown 特殊符号的文本，验证前端防 XSS 注入渲染。

#### 2. 网络异常与容灾测试 (Resilience)
- [ ] **API 响应超时**：模拟后端响应时间超过 30 秒，验证前端的超时提示与重试机制。
- [ ] **断网恢复**：在对话过程中断开网络连接，验证前端错误边界（Error Boundary）捕获及恢复网络后的重连逻辑。
- [ ] **大模型服务宕机**：强制关闭 OpenAI API 访问，验证 `CounselorOnboard` 页面是否能平滑降级并展示 Fallback 默认报告。

#### 3. 状态持久化与并发测试
- [ ] **多开标签页**：在同一浏览器打开多个 Chat 页面，验证状态同步与 Session ID 隔离机制。
- [ ] **页面刷新恢复**：在对话中途刷新页面，验证历史聊天记录是否丢失（当前为内存存储，需评估是否引入 LocalStorage 或 IndexedDB）。

### Phase 4: 性能与安全评估 (待执行)

#### 1. 性能指标 (Performance)
- [ ] **首屏加载时间 (FCP/LCP)**：使用 Lighthouse 测试首屏加载，目标 LCP < 1.5s。
- [ ] **长列表渲染**：模拟加载 500+ 条历史聊天记录，验证 React 虚拟列表（Virtual List）渲染性能，确保滚动不卡顿。
- [ ] **内存泄漏检查**：在 `CounselorOnboard` 页面反复进入和退出 30 分钟倒计时，使用 Chrome DevTools 检查内存占用是否持续增长。

#### 2. 安全合规 (Security)
- [ ] **敏感词过滤**：测试输入包含政治、暴力等敏感词汇时，系统是否具备基础的拦截或合规提示机制。
- [ ] **API 鉴权**：验证后端 API 接口是否容易被恶意脚本刷量（当前缺少 Rate Limiting 与 JWT 鉴权）。

---

## 📝 交付说明

本测试计划旨在为后续的迭代提供明确的质量保障方向。下一位 AI 工程师在接手时，应优先完成 Phase 3 中的“网络异常与容灾测试”，特别是大模型接口不稳定的降级处理，以提升系统的健壮性。
